// ============================================================
//  KELS 홈페이지 관리자 설정 파일
//  이 파일 하나에서 사이트의 모든 주요 설정을 변경할 수 있습니다.
//  자세한 사용법은 README.md 를 참고하세요.
// ============================================================

// ─────────────────────────────────────────
// 1. 소셜 링크 (푸터 아이콘 & 홈 Discord 버튼)
// ─────────────────────────────────────────
export const socialLinks = {
  discord: "https://discord.gg/your-invite-code",
  linkedin: "https://www.linkedin.com/company/kels-network",
} as const;

// ─────────────────────────────────────────
// 2. 사이트 연도 표기 (푸터 & 운영진 페이지)
// ─────────────────────────────────────────
export const siteYear = "2026";

// ─────────────────────────────────────────
// 3. 브랜드 색상 (Light 모드)
//    oklch(밝기 채도 색조) 형식 사용
//    온라인 변환기: https://oklch.com
// ─────────────────────────────────────────
export const lightColors = {
  // 페이지 배경
  background: "oklch(0.99 0.004 95)",
  // 기본 텍스트
  foreground: "oklch(0.18 0.02 264)",
  // 주 색상 (버튼, 링크 강조 등) — 현재: 딥 네이비
  primary: "oklch(0.32 0.09 264)",
  // 보조 강조 색상 (accent) — 현재: 연한 골드/옐로우
  accent: "oklch(0.94 0.05 95)",
  // 배경 글로우 색상 — 현재: 골드
  glow: "oklch(0.94 0.06 95)",
} as const;

// ─────────────────────────────────────────
// 4. 브랜드 색상 (Dark 모드)
// ─────────────────────────────────────────
export const darkColors = {
  background: "oklch(0.16 0.02 264)",
  foreground: "oklch(0.97 0.003 247)",
  primary: "oklch(0.85 0.07 90)",
  accent: "oklch(0.3 0.05 90)",
  glow: "oklch(0.78 0.08 92)",
} as const;

// ─────────────────────────────────────────
// 5. 로고 색상
// ─────────────────────────────────────────
export const logoColors = {
  // 배경 사각형 색상 (현재: 딥 네이비)
  background: "#16224d",
  // K, L, S 글자 색상
  letterColor: "#ffffff",
  // E 글자 강조 색상 (현재: 골드)
  accentLetter: "#f3c64b",
} as const;
